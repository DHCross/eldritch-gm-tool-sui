import React from 'react'
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card"

export default function PlayerCharacterGenerator() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Player Character Generator</CardTitle>
        <CardDescription>Create detailed player characters with full statistics</CardDescription>
      </CardHeader>
      <CardContent>
        <p>Player character generator functionality coming soon...</p>
      </CardContent>
    </Card>
  )
}